#!/usr/bin/env ruby

begin
  require "redsquare"
  Redsquare::App.run!
rescue LoadError => e
  require "rubygems"
  path = File.expand_path "../../lib", __FILE__
  $:.unshift(path) if File.directory?(path) && !$:.include?(path)
  require "redsquare"
  Redsquare::App.run!
end
